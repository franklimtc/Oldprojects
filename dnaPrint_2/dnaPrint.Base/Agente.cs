namespace dnaPrint.Base
{
    public class Agente
    {
        public enum Status { Ativo, Inativo};
        public enum Configuracoes { servidor, porta, usuario, senha, admin, adminPassword, tipoAgente, tipoBanco, StatusAtivacao, DataAtivacao };
    }
}
